/**
 * SettingsPage — no longer used by the router directly.
 * Sub-routes now point to BusinessProfile, MemberManagement, StoreManagementPage,
 * QRISConfig, and OutboxPage individually.
 * Navigation between sub-sections is handled by the NavBar sub-nav row.
 */
export default function SettingsPage() {
  return null;
}
