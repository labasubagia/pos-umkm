/**
 * ReportsPage — no longer used by the router directly.
 * Sub-routes now point to DailySummary, SalesReport, GrossProfitReport,
 * and CashReconciliation individually.
 * Navigation between sub-sections is handled by the NavBar sub-nav row.
 */
export default function ReportsPage() {
  return null;
}
