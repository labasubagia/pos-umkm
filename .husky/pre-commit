# 1. secretlint + biome 

# on staged files only (via lint-staged)
## npx lint-staged

# run manually
npx secretlint
npm run lint

# 2. Full TypeScript type check (whole project — cross-file inference requires it)
npm run check