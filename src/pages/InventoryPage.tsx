/**
 * InventoryPage — no longer used by the router directly.
 * Sub-routes now point to StockOpname and PurchaseOrders individually.
 * Navigation between sub-sections is handled by the NavBar sub-nav row.
 */
export default function InventoryPage() {
  return null;
}
