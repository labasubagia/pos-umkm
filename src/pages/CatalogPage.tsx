/**
 * CatalogPage — no longer used by the router directly.
 * Sub-routes now point to ProductList, CategoryList, CSVImport individually.
 * Navigation between sub-sections is handled by the NavBar sub-nav row.
 */
export default function CatalogPage() {
  return null;
}
